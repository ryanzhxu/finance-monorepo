[project]
name = "shared"
version = "0.1.0"
description = "Shared contracts and utilities for finance-monorepo services."
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7.0",
    "PyYAML>=6.0.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
